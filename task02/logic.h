#include <iostream>
#include <string>
using namespace std;

int findSum(int* array, int size);
int nonZeroElements(int* array, int size);
int arethmeticMean(int* array, int size);
void randomArray(int* array, int size, int maxVal, int minVal);
